using UnityEngine;
using System.Collections;

public class ModelFaceHandler : MonoBehaviour {

//	public GameObject[] faceTextures;
//	private GameObject currentFaceTexture;
//	private int currentIndex = 0; // for checks

	void Awake() {
//		foreach (GameObject g in faceTextures) {
//			g.SetActive (false);
//		}
	}

	public void SetFaceIndex(int index) {
//		if(currentFaceTexture != null && currentFaceTexture.activeSelf && index == currentIndex) return;
//		currentIndex = index;
//		if(currentFaceTexture != null) currentFaceTexture.SetActive (false);		
//		currentFaceTexture = faceTextures [index];
//		currentFaceTexture.SetActive (true);
	}
}